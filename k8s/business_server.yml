apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: business-server
  template:
    metadata:
      labels:
        app: business-server
    spec:
      containers:
        - name: business-server
          image: msa_project/business-server:latest
          ports:
            - containerPort: 8081
          env:
            - name: DB_HOST
              value: "mysql"
            - name: DB_PORT
              value: "3306"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: global-secrets
                  key: jwt-key
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: global-secrets
                  key: db-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: global-secrets
                  key: db-password
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
---
apiVersion: v1
kind: Service
metadata:
  name: business-server
spec:
  type: ClusterIP
  selector:
    app: business-server
  ports:
    - port: 8081
      targetPort: 8081
